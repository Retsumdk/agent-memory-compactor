{
  "name": "agent-memory-compactor",
  "description": "Background service for semantic summarization and compaction of long-term agent memories to maintain context efficiency",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}